console.log('=== Checking localStorage for Ma Lu data ==='); const data = JSON.parse(localStorage.getItem('dental_clinic_data') || '{}'); console.log('Full localStorage data:', data); if (data.appointments) { Object.keys(data.appointments).forEach(date => { const dayApps = data.appointments[date]; if (dayApps && dayApps.length > 0) { dayApps.forEach(app => { if (app.patientName && app.patientName.toLowerCase().includes('ma lu')) { console.log('Found Ma Lu appointment:', app); console.log('Date:', date); } }); } }); } else { console.log('No appointments found in localStorage'); }

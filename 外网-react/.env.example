# Firebase Configuration
# Copy this file to .env.local and fill in your actual values
# .env.local is gitignored and will not be committed to version control

# Note: Firebase API keys are safe to expose in client-side code
# Security is controlled by Firestore Security Rules and HTTP Referrer restrictions
# See: https://firebase.google.com/docs/projects/api-keys

VITE_FIREBASE_API_KEY=your_firebase_api_key_here
VITE_FIREBASE_AUTH_DOMAIN=your_project_id.firebaseapp.com
VITE_FIREBASE_PROJECT_ID=your_project_id
VITE_FIREBASE_STORAGE_BUCKET=your_project_id.firebasestorage.app

# Optional: Admin dashboard URL (new 内网-react)
# Example (dev): /内网/
# Example (prod): https://intranet.yourdomain.com/
VITE_ADMIN_DASHBOARD_URL=
